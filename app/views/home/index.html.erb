<style type="text/css">
  body {
  background: black;
  overflow: hidden;
}
p{
  color: white;
}
</style>
  <body>
      <div id="particles">
        <div id="intro">

          <div class="pc">
              <div class="pc_display" >
                <div class="pc_title">
                  
                  
                      
                    
                          <% if user_signed_in? %>
                <div class="pc_title_move">
                          <h1><%= link_to "Enter",articles_index_path, :style=>"color:white;" %></h1>
                </div>
                        <p><div class="logout"><%= link_to "Log out", destroy_user_session_path, method: :delete, :style=>"color:red;" %></div> </p>

                        <% else %>
                       
                        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                        
                      
                            <p>username:
                            <%= f.email_field :email, autofocus: true %></p>
        

    
                            <p>password:
                            <%= f.password_field :password, autocomplete: "off" %></p>
                      <p><%= f.submit "Log in" %></p> 
                          <% end -%>
                           


                      <% end %>
  
                </div>
              </canvas>
          </div>

        </div>
        </div>
    </div>
  </body>